export type AppVersion = 'Blent' | 'boost';

// Change this to 'boost' to enable full features for everyone
// When set to 'Blent', only admins see 'boost' features
export const APP_VERSION: AppVersion = 'Blent';
